[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd app && python -m gunicorn api.main:app --bind 0.0.0.0:$PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10